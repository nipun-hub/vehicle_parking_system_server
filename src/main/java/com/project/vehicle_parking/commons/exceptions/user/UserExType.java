package com.project.vehicle_parking.commons.exceptions.user;

public enum UserExType {
    USER_NOT_FOUND,
    EMAIL_ALREADY_EXIST,
    PROFILE_PICTURE_NOT_VALID,
    USERNAME_ALREADY_EXIST,
    PHONE_ALREADY_EXIST
}
